import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.realtime.app',
  appName: 'realtime-innovation-employee-app',
  webDir: 'www'
};

export default config;
